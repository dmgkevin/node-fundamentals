//Buffer => Representacao de um espaco na memoria do pc (+ performatico)
//Aloca na memoria em binario, facilitando o acesso

const buf = Buffer.from("ok") //fica representado em hexadecimal

console.log(buf.toJSON())